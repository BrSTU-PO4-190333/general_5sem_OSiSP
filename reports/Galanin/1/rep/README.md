## Make PDF

Build a project:

```bash
docker-compose up
```

Result:
- `_build/main.pdf`

Rebuild the project:

```bash
docker-compose up
```

At the end of the work:

```bash
docker-compose down
```

## Cleaning the project

```bash
make clean
```
